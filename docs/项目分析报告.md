# 3D识别比赛项目分析报告

> 基于比赛规则 v2026、当前工程代码扫描、开源参考项目对比、香橙派AI Pro手册分析
> 分析日期：2026-07-12  |  更新日期：2026-07-14（香橙派实测环境配置 + Round 1 端到端测试）

---

## 〇、香橙派实测环境配置（2026-07-14）

### 0.1 网络拓扑

```
Windows PC (192.168.1.66)  ←──TCP 6666──   Orange Pi (192.168.1.67)
  运行 judgeGui.exe                           运行 识别.sh → huanshibest.py
  (裁判盒评分)                                  (ORBBEC相机 + YOLO检测)
```

### 0.2 依赖安装记录

经过 2026-07-14 香橙派 HDMI 桌面实测，以下环境问题已解决：

| 步骤 | 问题 | 解决命令 | 状态 |
|------|------|----------|:--:|
| 1 | `cv2.imshow` 不存在 | `pip install opencv-python`（替换 headless 版） | ✅ |
| 2 | Qt 插件冲突 | `rm -rf ~/.local/lib/python3.8/site-packages/cv2/qt/` | ✅ |
| 3 | SSH 无法弹出 GUI | 识别.sh 中加 `DISPLAY=:0` | ✅ |
| 4 | 裁判盒 IP 不匹配 | `config.py` 中 `JUDGE_HOST='192.168.1.66'` | ✅ |
| 5 | 路径变更 | 部署路径改为 `~/Desktop/Round/ultralytics-main/` | ✅ |

### 0.3 关键文件路径（香橙派实测路径）

```
/home/HwHiAiUser/Desktop/
├── 识别.sh                              ← chmod +x, 含 DISPLAY=:0
├── result_r/                            ← 结果输出
└── Round/ultralytics-main/
    ├── config.py                        ← JUDGE_HOST=192.168.1.66, HOME_DIR=~/Desktop/Round/ultralytics-main
    ├── huanshibest.py                   ← 主入口
    ├── detector.py                      ← YOLO+相机+OCR
    ├── gui.py                           ← PyQt5 GUI + 流程
    ├── network.py                       ← 裁判盒 TCP 通讯
    └── ultralytics/utils/patches.py     ← opencv imshow fallback 已修复
```

### 0.4 Round 1 端到端测试结果

| 阶段 | 状态 | 备注 |
|------|:--:|------|
| 网络配置 (eth0=192.168.1.67) | ✅ | 静态IP已固定 |
| 相机初始化 (Astra Pro Plus) | ✅ | 彩色+深度双流, USB 2.0 模式 |
| 相机预热 (30帧) | ✅ | Frame queue full 警告，正常现象 |
| YOLO 模型加载 (best4.pt) | ✅ | 8 类, 6MB |
| PyQt5 GUI 弹出 | ✅ | HDMI 显示器正常显示 |
| 多帧检测 (20s) | ⚠️ | ARM CPU 推理 ~568ms/帧, 全部 `no detections` |
| 结果文件生成 | ✅ | NEEPU-VF-R1.txt 已生成 (空结果) |
| 裁判盒 TCP 连接 | ❌ | 裁判盒未启动导致 `[网络] 未连接` |

### 0.5 已知待解决问题

| 问题 | 原因 | 优先级 |
|------|------|:--:|
| YOLO 全部 no detections | conf=0.50 太高 OR 模型不匹配当前物品 | **P0** |
| ARM CPU 推理慢 (568ms) | 未用 NPU | P1 |
| 模型仅 8 类 | 训练数据为空 | **P0** |
| OCR 未安装 | Tesseract 和 pytesseract 未装 | P1 |
| 相机 USB 2.0 模式 | 可能未插 USB 3.0 蓝色口 | P2 |

---

## 一、项目结构概览

```
3D_Recongise/
├── ultralytics-main/          # 主工程（部署于香橙派 /home/HwHiAiUser/）
│   ├── huanshibest.py         # ★ 主识别脚本（识别.sh启动入口）
│   ├── ocr_module.py           # ★ OCR模块（LightweightOCR + SimpleTemplateOCR）【NEW】
│   ├── huanshi.py / huanshi1.py / huanshibest1.py  # 幻视变体
│   ├── huanjue.py / huanjue1.py / huanjuebest.py   # 幻决变体
│   ├── detect.py              # 检测脚本
│   ├── train.py               # 训练脚本（仅模板）
│   ├── test.py ~ test9.py     # 测试脚本
│   ├── best.pt ~ best6.pt     # 训练模型权重（~6MB，YOLOv8n级别）
│   ├── yolo11n.pt / yolo11s.pt / yolov8n.pt  # 基础模型
│   ├── zgjqr2024best.pt / zgjqr2024last.pt   # 2024年中国机器人大赛模型
│   ├── runss/                 # 运行输出目录（labels/, d/, result/, video）
│   └── ultralytics/           # YOLO库（修改版）
├── pyorbbecsdk-main/          # 奥比中光相机SDK
│   ├── detect.py              # SDK检测脚本
│   └── sdk/                   # 底层SDK库
├── crc2025_3d_detect_dyl-main/ # 开源参考项目（GitHub: xensedyl/crc2025_3d_detect_dyl）
│   ├── detect_qt.py / detect_qt2.py  # Qt GUI识别程序
│   ├── crc_2025.yaml          # 训练配置（10类）
│   ├── shibie1.sh / shibie2.sh # 启动脚本
│   ├── weights/crc2024best.pt / crc2024last.pt  # 2024年模型
│   └── log/                   # 日志和输出
├── result_r/NEEPU-VF-R2.txt   # 比赛结果文件（当前为空：仅START/END）
├── 识别.sh                    # 启动脚本
└── OrangePi_AI_Pro_昇腾_用户手册_v1.2.pdf  # 香橙派手册（207页）
```

---

## 二、当前实现 vs 比赛规则对照分析

### 2.1 识别流水线（当前实现 v2 — feat/ocr 分支已更新）

```
相机启动(ORBBEC Astra Pro Plus, RGBD)
    ↓
深度对齐 + 时域滤波(TemporalFilter α=0.7)
    ↓
YOLO检测(best4.pt, conf_thres=0.50, iou=0.45)
    │
    ├── CAxxx~CDxxx (已知16类) → 直接使用名称
    │
    └── Wxxx (未知物品) → cls_id ∈ _w_class_ids?
            ├── OCR可用 → 裁剪ROI → CLAHE增强 → OTSU二值化 → Tesseract OCR
            │                                              │
            │                                       匹配SUBCLASS_KEYWORDS
            │                                              │
            │                                    成功 → W01/W02/W10...
            │                                    失败 → 保持YOLO分类名
            │
            └── OCR不可用 → 保持YOLO外观分类
    ↓
深度过滤(100-1600mm范围筛选)
    ↓
逐帧检测 → 多帧投票(min_occurrences=5, 取众数)
    ↓
Round 1: process_detection_cycle(current_table=1)
Round 2: process_round2_full() → 旋转→检测→旋转→检测→旋转→检测
    ↓
生成结果文件: Goal_ID={name};Num={count};Table={table_id}
    ↓
TCP发送至裁判盒(192.168.1.66:6666)
```

### 2.2 规则完成度矩阵

| 规则要求 | 当前状态 | 完成度 | 关键差距 |
|----------|----------|--------|----------|
| RGBD相机(ORBBEC Astra Pro Plus) | ✅ 已实现 | 100% | - |
| 香橙派AI Pro(8T NPU, 16G RAM) | ⚠️ NPU环境就绪 | **50%** | **CANN/AscendCL已安装**，推理仍用CPU，待模型转换 |
| YOLO物体检测 | ⚠️ 仅8类 | **44%** | **7/13扫描: 所有模型(best4/5/6/bestt/bbest.pt)均仅8类** |
| 深度距离过滤 | ✅ 已实现 | 80% | 深度范围需按比赛现场调整 |
| 多帧投票去噪 | ✅ 已实现 | 85% | min_occurrences=5可调 |
| TCP通讯(裁判盒) | ✅ 已实现 | **95%** | **7/14 裁判盒协议已确认**（judgeGui_240328已到手），DataType 0/1/2/3全部明确 |
| start.sh一键启动 | ✅ 已实现 | 100% | - |
| PyQt5 GUI + 关闭按钮 | ✅ 已实现 | 100% | - |
| **结果格式含桌台编号(Table)** | ✅ 已实现 | **100%** | Goal_ID={name};Num={n};Table={t} |
| **未知物品文本识别(OCR)** | ✅ 已实现 | **80%** | LightweightOCR(Tesseract)集成完毕，待实地测试 |
| **相机旋转控制(Round 2)** | ✅ 框架完成 | **70%** | DataType 3 信号协议已明确，"0000"发送 |
| **两轮比赛自适应** | ✅ 已实现 | **100%** | RoundConfig类 + --round CLI参数 + 识别.sh传参 |
| **识别开始/结束信号** | ✅ 协议已明确 | **70%** | **7/14 DataType 0=开始计时, DataType 1=结束+传结果** |
| **资格认证视频录制** | ✅ 已实现 | **100%** | `record_video_opi.py` — cv2.VideoCapture+YOLO |
| **模型大小/加载时间优化** | ❌ 未实现 | **0%** | 2025新规：模型加载时间计入识别时间分 |
| 全部16类已知物品 | ❌ 仅8类 | **50%** | 训练配置18类已完备，**数据集为空(0张)**，模型仅8类 |
| 相机参数自动恢复 | ❌ 未实现 | 0% | 规则要求各队初始化时自行加载参数 |
| 动态MinTime/MaxTime配置 | ⚠️ 部分 | **60%** | MinTime/MaxTime 值已从裁判盒文档确认 |

### 2.3 当前输出格式 vs 规则要求

**当前输出格式**（huanshibest.py feat/table-number分支，已修复）：
```
START
Goal_ID=CA001;Num=2;Table=1
Goal_ID=CB003;Num=1;Table=1
Goal_ID=W01;Num=1;Table=2
END
```

**原始输出格式**（master分支，已废弃）：
```
START
Goal_ID=CA001;Num=2
Goal_ID=CB003;Num=1
END
```

**裁判评分表**（表8）包含字段：`ID | Num | Table`

**结论**：✅ Table编号字段已添加。Round 1 固定Table=1；Round 2 按旋转顺序Table=1/2/3。注意旋转指令协议格式需QQ群确认后微调。

---

## 三、开源参考项目分析 (crc2025_3d_detect_dyl)

### 3.1 项目概况
- GitHub: https://github.com/xensedyl/crc2025_3d_detect_dyl
- 作者: xensedyl
- 训练配置: 10类（每大类取2小类）

### 3.2 与当前工程的代码关系

当前 `huanshibest.py` 与参考项目 `detect_qt.py` 代码结构高度相似（~90%共享），主要差异：

| 特性 | 参考项目 (detect_qt.py) | 当前工程 (huanshibest.py) |
|------|------------------------|--------------------------|
| 检测时长 | 10秒 | 20秒 |
| 深度范围 | 1000-1800mm | 100-1600mm |
| 输出路径 | log/result/ | runss/result/ |
| 桌面路径 | /home/HwHiAiUser/crc2025_3d_detect_dyl/ | /home/HwHiAiUser/ultralytics-main/ |
| 模型权重 | crc2024best.pt | best4.pt |
| 队伍ID | (模板占位) | Y2507T1892934 |
| 轮次 | R1 | R2 |

### 3.3 参考项目的问题（当前工程已修复情况）

| 参考项目的缺陷 | 当前工程状态 |
|---------------|-------------|
| 没有Table编号输出 | ✅ 已修复 (feat/table-number) |
| 没有OCR文本识别 | ✅ 已修复 (feat/ocr) |
| 没有相机旋转控制 | ✅ 框架完成 (协议待确认) |
| 训练数据仅10类 | ⚠️ 待扩充至16类 |
| 没有使用NPU加速 | ⚠️ 待实现 |
| 硬编码R2、无Round 1支持 | ✅ 已修复 (RoundConfig + CLI) |

---

## 四、香橙派AI Pro硬件能力分析

### 4.1 关键硬件规格
- **NPU**: 8T INT8 算力（Ascend昇腾）
- **内存**: 16GB LPDDR4X
- **AI框架支持**: MindSpore 2.5, AscendCL (Python API)
- **操作系统**: Ubuntu (openEuler内核)
- **视频编解码**: 硬件H.264/H.265 (Ascend DVPP)
- **接口**: USB3.0 × 2, 千兆网口, HDMI, Type-C

### 4.2 当前NPU使用情况：环境已就绪（2026-07-14 更新）

**NPU部署进度：**

| 步骤 | 状态 | 说明 |
|------|------|------|
| ① CANN/AscendCL 环境安装 | ✅ **已完成** | Ascend AI计算库就绪，`ascend-dmi` 可检测NPU |
| ② ONNX 模型导出 | ❌ 待做 | `YOLO('best.pt').export(format='onnx')` |
| ③ ATC 模型转换 | ❌ 待做 | ONNX → Ascend OM 格式 |
| ④ AscendCL 推理集成 | ❌ 待做 | 替换 `detector.py` 中的 PyTorch 推理路径 |

代码中目前仍使用ARM CPU推理：
```python
import torch  # 使用PyTorch，运行在ARM CPU上
model = YOLO(weights)  # 标准Ultralytics推理，无NPU后端
```

### 4.3 NPU部署路径（4步走）

```
① CANN/AscendCL安装 ✅ → ② ONNX导出 → ③ ATC模型转换 → ④ AscendCL推理
   (已完成)                   .pt→.onnx      .onnx→.om       API集成到detector.py
```

---

### 4.4 步骤②：ONNX 模型导出

**目标**: 将 PyTorch 模型转换为 ONNX 通用格式。

```bash
cd /home/HwHiAiUser/Desktop/Round/ultralytics-main

# 导出 ONNX（在香橙派上直接跑，不需要 GPU）
python3 -c "
from ultralytics import YOLO
model = YOLO('best4.pt')
model.export(format='onnx', imgsz=640, opset=11)
print('ONNX 导出完成 → best4.onnx')
"
```

**参数说明**:
| 参数 | 值 | 原因 |
|------|-----|------|
| `format` | `onnx` | 目标格式 |
| `imgsz` | `640` | 比赛用 640×640 输入 |
| `opset` | `11` | ATC 工具推荐 opset 版本（兼容性最好） |

**验证**:
```bash
ls -la best4.onnx          # 应存在，约 10-15 MB
python3 -c "import onnx; onnx.checker.check_model('best4.onnx'); print('ONNX 有效')"
```

**常见问题**:
| 错误 | 原因 | 解决 |
|------|------|------|
| `ModuleNotFoundError: onnx` | 未安装 onnx | `pip install onnx onnxruntime` |
| `opset version not supported` | opset 版本过高 | 改为 `opset=11` |
| 动态 batch 维度 | ATC 需要固定 batch | 添加 `dynamic=False`（默认） |

---

### 4.5 步骤③：ATC 模型转换

**目标**: 将 ONNX 转换为 Ascend NPU 原生格式 (.om)。

#### 4.5.1 确认环境

```bash
# 确认 CANN 已安装
source /usr/local/Ascend/ascend-toolkit/set_env.sh
atc --version            # 应显示版本号

# 确认 NPU 可见
npu-smi info             # 应显示 NPU 芯片信息
```

#### 4.5.2 创建 ATC 配置文件

创建 `/home/HwHiAiUser/ultralytics-main/atc_insert_op.cfg`:

```
# ATC 算子插入配置
aipp_op {
    aipp_mode: static
    input_format: RGB888_U8
    csc_switch: false
    rbuv_swap_switch: false
    mean_chn_0: 0
    mean_chn_1: 0
    mean_chn_2: 0
    min_chn_0: 0.0
    min_chn_1: 0.0
    min_chn_2: 0.0
    var_reci_chn_0: 0.003921568627451
    var_reci_chn_1: 0.003921568627451
    var_reci_chn_2: 0.003921568627451
}
```

> 如果不需要 AIPP 预处理，可以不带 `--insert_op_conf`。

#### 4.5.3 执行转换

```bash
# 设置环境
source /usr/local/Ascend/ascend-toolkit/set_env.sh

# 转换（不带 AIPP，更简单，预处理在 Python 侧做）
atc --model=best4.onnx \
    --framework=5 \
    --output=best4 \
    --soc_version=Ascend310B4 \
    --input_shape="images:1,3,640,640" \
    --input_format=NCHW \
    --output_type=FP16 \
    --log=error
```

**参数详解**:

| 参数 | 值 | 说明 |
|------|-----|------|
| `--model` | `best4.onnx` | 输入的 ONNX 模型 |
| `--framework` | `5` | 5=ONNX |
| `--output` | `best4` | 输出文件名（自动加 .om） |
| `--soc_version` | `Ascend310B4` | **关键！** 香橙派 AI Pro 的芯片型号 |
| `--input_shape` | `images:1,3,640,640` | 固定 batch=1, 3通道, 640×640 |
| `--input_format` | `NCHW` | PyTorch 标准格式 |
| `--output_type` | `FP16` | 半精度推理（NPU 原生支持，速度快） |

**验证**:
```bash
ls -la best4.om           # 应存在，约 3-6 MB（FP16 比 FP32 小一半）
```

**常见问题**:
| 错误 | 原因 | 解决 |
|------|------|------|
| `soc_version not supported` | 芯片型号写错 | 确认：香橙派AI Pro 用 `Ascend310B4` |
| `Input node not found` | ONNX 输入节点名不对 | 先用 Netron 查看 ONNX: `pip install netron` |
| `Dynamic shape not supported` | ONNX 有动态维度 | 导出时加 `dynamic=False` |
| OOM | 模型太大 | 试 `--output_type=FP16` 或减小 imgsz |
| ATC 命令不存在 | 环境未 source | `source /usr/local/Ascend/ascend-toolkit/set_env.sh` |

---

### 4.6 步骤④：AscendCL 推理集成到 detector.py

**目标**: 在 `YoloOrbbecDetector` 中添加 NPU 推理路径，替代 PyTorch ARM CPU 推理。

#### 4.6.1 验证 NPU 基本推理（先单独测试）

创建 `/home/HwHiAiUser/ultralytics-main/test_npu.py`:

```python
"""
NPU 推理验证脚本
用法: python3 test_npu.py
"""
import numpy as np
import cv2
import time
import os
import sys

# 添加 CANN Python API 路径
sys.path.append('/usr/local/Ascend/ascend-toolkit/latest/python/site-packages')
import acl

class NPUDetector:
    """最简 NPU 检测器——验证 NPU 推理管线是否正常"""

    def __init__(self, om_path, imgsz=640):
        self.imgsz = imgsz
        self.device_id = 0

        # 1. 初始化 ACL
        ret = acl.init()
        assert ret == 0, f"ACL init failed: {ret}"

        # 2. 打开设备
        ret = acl.rt.set_device(self.device_id)
        assert ret == 0, f"Set device failed: {ret}"

        # 3. 加载模型
        self.model_id, _ = acl.mdl.load_from_file(om_path)
        print(f"[NPU] 模型已加载: {om_path}")

        # 4. 获取模型描述
        self.model_desc = acl.mdl.create_desc()
        ret = acl.mdl.get_desc(self.model_desc, self.model_id)
        assert ret == 0

        # 5. 创建输入输出 Dataset
        self.input_dataset, self.output_dataset = acl.mdl.create_dataset()
        # 分配输入 buffer（大小从模型描述获取）
        input_size = acl.mdl.get_input_size_by_index(self.model_desc, 0)
        self.input_buffer, _ = acl.rt.malloc(input_size, acl.ACL_MEM_MALLOC_HUGE_FIRST)
        dataset_buffer = acl.create_data_buffer(self.input_buffer, input_size)
        _, ret = acl.mdl.add_dataset_buffer(self.input_dataset, dataset_buffer)
        assert ret == 0

        # 分配输出 buffer
        output_size = acl.mdl.get_output_size_by_index(self.model_desc, 0)
        self.output_buffer, _ = acl.rt.malloc(output_size, acl.ACL_MEM_MALLOC_HUGE_FIRST)
        out_dataset_buffer = acl.create_data_buffer(self.output_buffer, output_size)
        _, ret = acl.mdl.add_dataset_buffer(self.output_dataset, out_dataset_buffer)
        assert ret == 0

        print(f"[NPU] 输入大小: {input_size}, 输出大小: {output_size}")

    def preprocess(self, image):
        """预处理: BGR→RGB, resize→640×640, HWC→CHW, normalize to [0,1]"""
        img = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)
        img = cv2.resize(img, (self.imgsz, self.imgsz))
        img = img.astype(np.float32) / 255.0
        img = np.transpose(img, (2, 0, 1))  # HWC → CHW
        img = np.expand_dims(img, axis=0)    # → (1, 3, 640, 640)
        return np.ascontiguousarray(img)

    def infer(self, image):
        """单帧 NPU 推理"""
        # 预处理
        input_np = self.preprocess(image)

        # 拷贝到 NPU 内存
        acl.rt.memcpy(self.input_buffer, input_np.size * 4,
                      input_np.tobytes(), input_np.size * 4,
                      acl.ACL_MEMCPY_HOST_TO_DEVICE)

        # 执行推理
        ret = acl.mdl.execute(self.model_id, self.input_dataset, self.output_dataset)
        if ret != 0:
            raise RuntimeError(f"Model execute failed: {ret}")

        # 拷贝结果回主机
        output_size = acl.mdl.get_output_size_by_index(self.model_desc, 0)
        output_np = np.zeros(output_size // 4, dtype=np.float32)
        acl.rt.memcpy(output_np.ctypes.data, output_size,
                      self.output_buffer, output_size,
                      acl.ACL_MEMCPY_DEVICE_TO_HOST)

        return output_np

    def close(self):
        acl.rt.free(self.input_buffer)
        acl.rt.free(self.output_buffer)
        acl.mdl.unload(self.model_id)
        acl.mdl.destroy_desc(self.model_desc)
        acl.rt.reset_device(self.device_id)
        acl.finalize()


if __name__ == '__main__':
    print("[NPU 测试] 加载模型...")
    detector = NPUDetector('best4.om', imgsz=640)

    # 测试空推理（预热）
    print("[NPU 测试] 预热推理...")
    dummy = np.random.randint(0, 255, (640, 640, 3), dtype=np.uint8)
    for _ in range(3):
        _ = detector.infer(dummy)

    # 计时测试
    print("[NPU 测试] 计时推理 x100...")
    times = []
    for i in range(100):
        t0 = time.time()
        _ = detector.infer(dummy)
        times.append((time.time() - t0) * 1000)  # ms

    print(f"[NPU 测试] 平均: {np.mean(times):.1f}ms  |  "
          f"最小: {np.min(times):.1f}ms  |  最大: {np.max(times):.1f}ms")
    print(f"[NPU 测试] 预期 NPU 推理: ~15-20ms")
    print(f"[NPU 测试] 对比 CPU: ~120ms (YOLO11n on ARM)")

    detector.close()
    print("[NPU 测试] ✅ 完成")
```

运行验证:
```bash
cd /home/HwHiAiUser/Desktop/Round/ultralytics-main
python3 test_npu.py
```

> **预期输出**: 推理时间 ~15-20ms（相比 CPU 的 ~120ms 提速约 6-8 倍）

#### 4.6.2 后处理：YOLO 输出解码

NPU 直接输出的是原始张量 `(1, 84, 8400)`（以 YOLO11n 为例：80类+4box × 8400 anchors），需要 NMS 后处理。

创建 `/home/HwHiAiUser/ultralytics-main/npu_postprocess.py`:

```python
"""
NPU 推理后处理 — YOLO 输出解码 + NMS
"""
import numpy as np
import cv2


def nms(boxes, scores, iou_thres=0.45):
    """非极大值抑制（CPU 上做，很快）"""
    indices = cv2.dnn.NMSBoxes(
        boxes.tolist(), scores.tolist(),
        score_threshold=0.5, nms_threshold=iou_thres
    )
    if len(indices) == 0:
        return np.array([])
    return indices.flatten()


def decode_yolo_output(output_np, img_w, img_h, conf_thres=0.5, iou_thres=0.45):
    """
    解码 NPU 输出为检测框列表
    output_np: np.float32 shape (1, 84, 8400) — 以 YOLO11n 为例
    返回: [(x1, y1, x2, y2, conf, cls_id), ...]
    """
    # YOLO11n 输出格式: [batch, 4+nc, anchors]
    output = output_np[0]  # (84, 8400)
    nc = output.shape[0] - 4  # 类别数
    boxes_raw = output[:4, :]  # (4, 8400) — cx, cy, w, h
    scores_raw = output[4:, :]  # (nc, 8400)

    # 每个 anchor 取最大置信度类别
    max_scores = scores_raw.max(axis=0)  # (8400,)
    max_cls = scores_raw.argmax(axis=0)  # (8400,)

    # 置信度过滤
    mask = max_scores > conf_thres
    if not mask.any():
        return np.array([])

    boxes_raw = boxes_raw[:, mask]
    max_scores = max_scores[mask]
    max_cls = max_cls[mask]

    # cx,cy,w,h → x1,y1,x2,y2
    cx, cy, w, h = boxes_raw[0], boxes_raw[1], boxes_raw[2], boxes_raw[3]
    x1 = (cx - w / 2) * img_w / 640
    y1 = (cy - h / 2) * img_h / 640
    x2 = (cx + w / 2) * img_w / 640
    y2 = (cy + h / 2) * img_h / 640

    boxes = np.stack([x1, y1, x2, y2], axis=1)

    # NMS
    keep = nms(boxes, max_scores, iou_thres)
    if len(keep) == 0:
        return np.array([])

    result = np.column_stack([
        boxes[keep],
        max_scores[keep],
        max_cls[keep].astype(np.float32)
    ])
    return result
```

#### 4.6.3 集成到 detector.py（改动指南）

在 `detector.py` 的 `YoloOrbbecDetector` 类中添加 NPU 推理路径：

```python
# detector.py 顶部添加
import sys
sys.path.append('/usr/local/Ascend/ascend-toolkit/latest/python/site-packages')
import acl

class YoloOrbbecDetector:
    def __init__(self, weights=DEFAULT_WEIGHTS, device='0', half=False,
                 use_npu=True):  # ← 新增参数
        # ... 原有初始化代码 ...

        self.use_npu = use_npu
        self.npu_detector = None

        if self.use_npu:
            self._init_npu(weights)
        else:
            # 原有 PyTorch 初始化
            self.model = YOLO(weights)

    def _init_npu(self, weights):
        """初始化 NPU 推理"""
        om_path = weights.replace('.pt', '.om')
        if not os.path.exists(om_path):
            print(f"[检测器] .om 文件不存在: {om_path}, 回退到 CPU")
            self.use_npu = False
            self.model = YOLO(weights)
            return
        # 延迟导入 acl，避免在无 CANN 环境崩溃
        try:
            self.npu_detector = NPUDetector(om_path)
            print(f"[检测器] NPU 推理已启用: {om_path}")
        except Exception as e:
            print(f"[检测器] NPU 初始化失败: {e}, 回退到 CPU")
            self.use_npu = False
            self.model = YOLO(weights)

    def inference_image(self):
        """单帧检测 — 根据 use_npu 切换推理路径"""
        # ... 原有取帧/深度对齐代码 ...

        if self.use_npu and self.npu_detector is not None:
            # NPU 推理路径
            output = self.npu_detector.infer(color_image)
            h, w = color_image.shape[:2]
            boxes = decode_yolo_output(output, w, h,
                                       conf_thres=self.conf_thres)
        else:
            # 原有 PyTorch 推理路径
            results = self.model(color_image, conf=self.conf_thres,
                                iou=IOU_THRES, verbose=False)
            boxes = results[0].boxes.data.cpu().numpy() if len(results) > 0 else []

        # ... 后续深度过滤/投票逻辑不变 ...
```

### 4.7 NPU 部署实测结果 (2026-07-14)

#### 实测性能

| 指标 | ARM CPU (PyTorch) | NPU (Ascend 310B4) | 提升 |
|------|------------------|--------------------|:----:|
| 单帧推理 | 568ms | **20.4ms** | **28×** |
| 端到端帧率 | ~1.7 FPS | ~20 FPS | **12×** |
| .om 模型大小 | — | 6.5MB (FP16) | — |

#### 部署流水线

```
best4.pt → export_onnx.py → best4.onnx (11.7MB)
         ↓
         ATC: atc --model=best4.onnx --soc_version=Ascend310B4 --output_type=FP16
         ↓
         best4.om (6.5MB, FP16)
         ↓
         npu_detector.py → NPUDetector 类
         ↓
         detector.py → YoloOrbbecDetector (USE_NPU=True/False 一键切换)
```

#### CANN 7.0 API 适配关键

总结为 `memory/npu-configuration-guide.md`。

核心差异:
- `acl.rt.malloc(size, 0)` — 双参数, 返回 `(ptr, ret)` 元组
- `acl.rt.memcpy(dst,sz,src_ptr,sz,dir)` — 不接 bytes, 必须用 host 内存中转 (`ctypes.memmove`)
- `acl.mdl.execute(id,in,out)` — 3 参数 (无 stream)
- 所有整型常量已移除 — 硬编码 1=H2D, 2=D2H
- FP16 输出需 `view(np.float16) → astype(np.float32)`
- **NPU 初始化必须在推理线程** (PyQt5 跨线程问题)

#### 已知限制

| 问题 | 说明 |
|------|------|
| 实时画面标签/检测框不显示 | Qt 渲染管道问题, 待排查 |
| FP16 精度损失 | 置信度约降 ~0.05, CONF_THRES 调至 0.35 补偿 |
| 预处理必须 letterbox | 直接 resize 会拉伸变形, 坐标偏移 |

---

### 4.8 裁判盒软件 (judgeGui_240328)

#### 4.8.1 软件概要

| 项目 | 说明 |
|------|------|
| 文件 | `judgeGui.exe` (21.7 MB) |
| 平台 | Windows |
| 版本 | 2024-03-28 |
| 依赖 | Microsoft Excel (已激活) |
| 端口 | TCP 6666 |
| 缩放 | 系统缩放 100% |
| 目标 IP | **192.168.1.66** (Windows PC, 实测配置) |

#### 4.8.2 通信协议（二进制）

数据包结构: `| DataType (4B) | DataLength (4B) | Data (DataLength B) |`

| DataType | 含义 | 香橙派发送时机 | Data 内容 |
|----------|------|---------------|-----------|
| **0** | 队伍ID + **开始计时** | 识别开始(`识别.sh`启动后) | 队伍ID字符串 |
| **1** | 结果文件 + **停止计时** | 每轮识别完成 | TXT文件全部内容 |
| **2** | 结果文件 (工业测量) | — | TXT内容, 得分=0 |
| **3** | **转台旋转信号** | 每张桌台检测完毕 | `"0000"` |

#### 4.8.3 时序规则

```
DataType 0 发送 → 裁判盒开始计时
    ↓
DataType 3 发送 → 转台旋转（Round 2 每张桌后）
    ↓
DataType 1 发送 → 停止计时 + 传结果文件
```

| 轮次 | MinTime | MaxTime |
|------|---------|---------|
| 第1轮 识别 | 20s | 50s |
| 第1轮 测量 | 20s | 50s |
| 第2轮 识别 | 70s | 150s |
| 第2轮 测量 | 20s | 50s |

#### 4.8.4 计分规则（3D识别）

- 误检 (false positive): 每多1个扣0.02×(超出数量), 超40% 得0分
- 漏检 (false negative): 每多1个扣0.02×(超出数量), 超50% 得0分
- 同一 ID 出现两次 → 两条都 0 分
- 动态/静态物品交叉出现 → 0 分
- ID 格式: 字母+数字, 不要特殊符号

#### 4.8.5 目录结构

```
judgeGui_240328/
├── judgeGui.exe           # 主程序
├── Template_excel/        # 评分模板 (严禁覆盖)
│   ├── Distinguish_Score.xlsx    # 3D识别评分
│   ├── Distinguish_real.xlsx     # 3D识别真值
│   ├── score_dynamic.xlsx        # 动态测量评分
│   └── real_dynamic.xlsx         # 动态测量真值
├── Result/Backup/         # 结果备份
└── 裁判盒软件计分说明-20221113.docx  # 完整协议+计分文档
```

---

## 五、相机旋转云台 (Round 2 必需)

### 5.1 现有资源

| 文件 | 内容 |
|------|------|
| `2_相机旋转云台/转台制作.pdf` | 硬件组装图纸 |
| `2_相机旋转云台/rotateplatform2022/rotateplatform2022.ino` | Arduino 固件 (完整) |

### 5.2 固件功能分析

基于 `rotateplatform2022.ino`:

- **舵机控制**: PWM 驱动, 0-180° 范围, 平滑过渡 `delay(35)`
- **3 预设角度**: 存储在 EEPROM (ATmega 非易失), 地址 0/1/2
- **手动校准模式**: 按钮进入 RECORD 模式 → 旋钮微调 → 保存当前角度
- **串口指令** (9600 baud): `"1\n"` 转位置1 / `"2\n"` 转位置2 / `"3\n"` 转位置3
- **物理按键**: 3 个位置键 + 模式切换 + 微调左右
- **显示反馈**: TM1638 LED 数码管, 当前桌号亮起

### 5.3 集成到工程

```python
# network.py 新增
import serial

def send_rotate_serial(self, table_id):
    """通过串口发送旋转指令到 Arduino"""
    with serial.Serial('/dev/ttyUSB0', 9600, timeout=1) as ser:
        ser.write(f"{table_id}\n".encode())
    time.sleep(2)  # 等待舵机到位
```

### 5.4 待办

| 任务 | 说明 |
|------|------|
| 组装硬件 | Arduino Nano + 舵机 + 相机固定支架 |
| 烧录固件 | `rotateplatform2022.ino` |
| 3 角度校准 | 对应 3 张桌台位置, 存入 EEPROM |
| 串口集成 | `network.py` 添加 `send_rotate_serial()` |
| Round 2 联调 | 旋转→检测×3 全流程 |

---

## 六、提升与改进方案

### 5.1 数据采集层面

#### 5.1.1 18 类物品采购与采集计划 (7/18-7/21)

详见 `docs/P1_7月18-25日计划.md`

**采集标准**:
- 每类 3-5 个不同品牌/款式的变体
- 每个变体: 5 角度 × 4 光照 × 3 距离 ≈ 60 张
- 总计: 18 类 × 3 变体 × 60 张 ≈ **3,240 张**
- 含遮挡增补 + 未知物品: 目标 **~3,500 张**

**预算**: 训练物品采购 ~¥400

**工具**: LabelImg (标注) / Ultralytics 标注格式

#### 5.1.2 采集进度

| 大类 | 物品 | 状态 | 预计日期 |
|------|------|:--:|------|
| CA 日用品 | 刷子/耳机/水杯/衣架 | 🔜 | 7/18 |
| CB 副食品 | 巧克力/瓜子/火腿肠/薯片 | 🔜 | 7/19 |
| CC 饮料 | 罐装/瓶装/盒装牛奶/瓶装水 | 🔜 | 7/19 |
| CD 水果 | 桃子/苹果/香蕉/梨 | 🔜 | 7/20 |
| W 未知 | 书籍/带文字包装 | 🔜 | 7/20 |

| 改进项 | 当前状态 | 建议方案 | 优先级 |
|--------|----------|----------|--------|
| 物品类别覆盖 | 仅 8 类 | 采集全部 18 类 | **P0** |
| 多品牌变体 | 0 | 每类 3-5 变体 | **P0** |
| 多角度数据 | 有限 | 0°/30°/60°/90°/顶视 | P1 |
| 多背景数据 | 未知 | 纯色/混合色/图案背景各500张 | P1 |
| 光照变化 | 未知 | 日光/白光LED/黄光灯/混合 | P1 |
| 遮挡场景 | 未采集 | 20%-50%-70%遮挡比例训练集 | P1 |
| 干扰物数据 | 未采集 | 干扰贴纸+地面干扰物负样本 | P1 |
| 深度图数据 | 仅过滤用 | 训练RGBD双模态融合模型 | P2 |

### 5.2 训练策略层面

| 改进项 | 建议方案 | 预期收益 |
|--------|----------|----------|
| **数据增强** | Mosaic + MixUp + 随机背景替换 + 光照扰动 | 提升泛化能力 |
| **多尺度训练** | imgsz=320~960 随机尺度 | 适配1.0-1.8m距离变化 |
| **困难样本挖掘** | 对易混淆物品(如不同品牌饮料)增加训练权重 | 减少类别混淆 |
| **渐进式训练** | 先在COCO预训练 → 生活物品数据集 → 比赛特定物品 | 提升小样本类别精度 |
| **知识蒸馏** | YOLO11s(教师) → YOLO11n(学生，用于部署) | 精度-速度平衡 |
| **模型集成** | 训练3个不同初始化模型，推理时投票 | +1-3% mAP |
| **负样本训练** | 加入干扰贴纸和干扰物作为"背景"类 | 减少误检 |

### 5.3 OCR模块详析与优化路线

#### 5.3.1 当前实现（v1 — feat/ocr分支）

**方案**：Tesseract OCR（Google维护的开源引擎，预训练模型，无需自己训练）

```
图像帧 → YOLO检测到W类物品 → 裁剪ROI(扩展5px)
    → CLAHE对比度增强 → fastNlMeansDenoising去噪
    → OTSU自适应二值化 → Tesseract OCR(chi_sim+eng)
    → 关键词匹配(SUBCLASS_KEYWORDS字典) → 子类编号(W01/W02/...)
```

**优势**：
- 零训练成本：`apt install` 即用
- ARM原生：C++引擎，香橙派CPU直接运行
- 轻量级：内存占用 < 50MB
- 中英混合：`chi_sim+eng` 语言包覆盖比赛场景

**当前局限**：
- 弯曲表面（易拉罐）识别率下降
- 反光/强阴影下需要更强的预处理
- 关键词匹配是硬匹配，不支持模糊语义（"高等数学上册"匹配不到"数学"的问题不存在，因为用了子串匹配，但顺序敏感）
- Tesseract对非标准字体（艺术字、手写体）效果差

#### 5.3.2 OCR优化路线图

| 阶段 | 方案 | 改动量 | 效果提升 | 适用场景 |
|------|------|--------|----------|----------|
| **v1 当前** | Tesseract + 关键词匹配 | 已完成 | 基线 | 清晰印刷体 |
| **v1.1 快速** | 多尺度预处理管线 | 50行 | +15% 弯曲表面 | 比赛前微调 |
| **v1.2 快速** | 多帧OCR投票（同一物品多帧识别取众数） | 30行 | +10% 稳定性 | 利用现有多帧机制 |
| **v2 中期** | PaddleOCR-轻量版替换Tesseract | 替换引擎 | +25% 中文准确率 | 如果v1实地测试不理想 |
| **v3 远期** | YOLO+OCR联合微调（检测头加OCR分支） | 需训练 | +30%端到端 | 长期方案 |

#### 5.3.3 v1.1 多尺度预处理管线（建议赛前实施）

```python
def preprocess_multiscale(self, crop_bgr):
    """多尺度预处理：对同一ROI生成3个预处理变体，OCR后投票"""
    results = []
    # 变体1: CLAHE + OTSU (当前方案，适合平整表面)
    gray = cv2.cvtColor(crop_bgr, cv2.COLOR_BGR2GRAY)
    clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8,8))
    gray1 = clahe.apply(gray)
    _, bin1 = cv2.threshold(gray1, 0, 255, cv2.THRESH_BINARY+cv2.THRESH_OTSU)
    results.append(bin1)

    # 变体2: 自适应阈值 (适合光照不均)
    bin2 = cv2.adaptiveThreshold(gray1, 255,
        cv2.ADAPTIVE_THRESH_GAUSSIAN_C, cv2.THRESH_BINARY, 11, 2)
    results.append(bin2)

    # 变体3: 锐化 + 二值化 (适合模糊文字)
    kernel = np.array([[-1,-1,-1],[-1,9,-1],[-1,-1,-1]])
    sharp = cv2.filter2D(gray, -1, kernel)
    _, bin3 = cv2.threshold(sharp, 0, 255, cv2.THRESH_BINARY+cv2.THRESH_OTSU)
    results.append(bin3)

    return results  # OCR后投票选出最一致的文本
```

#### 5.3.4 v2 备选方案：PaddleOCR-轻量版

如果Tesseract实地测试准确率不足，迁移到PaddleOCR：

| 对比维度 | Tesseract (v1) | PaddleOCR-轻量版 (v2) |
|----------|---------------|----------------------|
| 中文准确率 | ~85% | ~95% |
| 安装大小 | ~20MB | ~50MB (mobile版) |
| 推理速度(ARM) | ~200ms/crop | ~400ms/crop |
| 弯曲文本 | ❌ 差 | ✅ 支持 |
| 角度校正 | 需额外处理 | ✅ 内置 |
| 依赖 | libtesseract(C++) | Paddle Lite(ARM优化) |

```bash
# PaddleOCR-轻量版安装（备选）
pip install paddlepaddle  # ARM版本需特殊wheel
pip install paddleocr
```

#### 5.3.5 关键词匹配优化（比赛前必做）

赛前QQ群公布未知物品小类后，更新 `SUBCLASS_KEYWORDS` 映射表：

```python
# ocr_module.py LightweightOCR.SUBCLASS_KEYWORDS
# 赛前根据QQ群公布的未知物品小类修改此表
# 原则: 关键词越长越优先匹配（内部已按长度降序）
SUBCLASS_KEYWORDS = {
    # === 书籍类（示例） ===
    '高等数学': 'W01',
    '数学分析': 'W01',
    '线性代数': 'W01',
    '大学英语': 'W02',
    '新概念英语': 'W02',
    '大学物理': 'W03',
    '有机化学': 'W04',
    # === 饮料包装（示例） ===
    '百事可乐': 'W10',
    '可口可乐': 'W10',
    '农夫山泉': 'W11',
    '怡宝': 'W11',
    # ... 更多按需添加
}

# 也可赛前现场调用 update_keywords() 动态添加
ocr.update_keywords({'C语言': 'W05', 'Python': 'W05'})
```

#### 5.3.6 其他学习方法

| 改进项 | 当前状态 | 建议方案 | 优先级 |
|--------|----------|----------|--------|
| **深度特征融合** | 仅距离过滤 | RGB+Depth早期融合或晚期融合，增强3D感知 | P1 |
| **多任务学习** | 仅检测 | 检测+细粒度分类+OCR识别多任务联合 | P2 |
| **少样本学习** | 无 | 未知物品公布后快速适配（元学习/原型网络） | P2 |
| **自监督预训练** | 无 | MAE/DINO在大量目标域图片上预训练backbone | P3 |
| **主动学习** | 无 | 标记低置信度样本加入训练集 | P3 | |

### 5.4 模型选择与修改层面

| 选项 | 参数量 | 推理速度(ARM CPU) | 推理速度(NPU) | mAP | 推荐 |
|------|--------|-------------------|---------------|-----|------|
| YOLOv8n | 3.2M | ~150ms | ~20ms | 37.3 | 当前基线 |
| YOLO11n | 2.6M | ~120ms | ~18ms | 39.5 | ⭐ NPU首选 |
| YOLO11s | 9.4M | ~250ms | ~35ms | 47.0 | 精度优先 |
| YOLOv10n | 2.3M | ~110ms | ~16ms | 38.5 | 速度优先 |
| RT-DETR | 20M+ | ~400ms | N/A | 53.0 | 不建议(太重) |
| PP-YOLOE+ | 7.9M | ~200ms | ~30ms | 42.0 | 备选 |

**推荐模型策略**：
1. **主模型**: YOLO11n → 经ATC转换部署到Ascend NPU
2. **辅助模型**: YOLO11s → 用于困难类别精调后的知识蒸馏

### 5.5 架构改进（代码层面）

#### 5.5.1 增加Table编号功能（P0 - 致命缺陷）

```python
# 当前（错误）:
f.write(f"Goal_ID={word};Num={count}\n")

# 应为（参考规则要求的格式）:
# 需在检测时判断物品属于哪个目标台，并输出Table编号
f.write(f"Goal_ID={word};Num={count};Table={table_id}\n")
```

实现方案：
- **Round 1**: 仅1张目标台，Table=1
- **Round 2**: 3张目标台。通过**相机旋转角度**判断当前朝向的目标台，或通过**深度信息+空间位置**聚类区分物品所属的目标台

#### 5.5.2 添加相机旋转控制（P0 - Round 2必需）

```python
# 需添加相机旋转指令发送
def send_rotate_command(self, angle):
    """向裁判盒发送相机旋转指令"""
    self.send_string(2, f"ROTATE:{angle}")  # 具体协议待QQ群公布

# Round 2流程:
# 1. 旋转到Table 1位置 → 检测
# 2. 旋转到Table 2位置 → 检测  
# 3. 旋转到Table 3位置 → 检测
```

#### 5.5.3 添加OCR文本识别模块（P0）

```python
from paddleocr import PaddleOCR  # 或 easyocr

class OCRRecognizer:
    def __init__(self):
        self.ocr = PaddleOCR(lang='ch', use_angle_cls=True)
    
    def recognize_text(self, image, bbox):
        """对检测到的未知物品区域进行OCR"""
        crop = image[bbox[1]:bbox[3], bbox[0]:bbox[2]]
        results = self.ocr.ocr(crop)
        # 根据识别到的文本判断Wxxx子类
        return extracted_text
```

#### 5.5.4 NPU推理加速（P1）

```python
# 方案1: 使用AscendCL Python API
import ascendcl
# 加载ATC转换后的.om模型
model = ascendcl.Model('yolo11n.om')
output = model.infer(input_tensor)

# 方案2: 使用MindSpore Lite
import mindspore_lite as mslite
model = mslite.Model()
model.build_from_file('yolo11n.mindir')
```

#### 5.5.5 信号协议完善（P1）

```python
# 当前仅发送: 队伍ID(类型0) + 结果文件(类型1)
# 需添加:
self.send_string(2, "START")     # 开始识别信号
self.send_string(3, "ROTATE:30")  # 相机转动指令(角度)
self.send_string(4, "END")       # 结束识别信号
```

#### 5.5.6 两轮自适应配置（P1）

```python
class RoundConfig:
    ROUND1 = {
        'num_tables': 1,
        'rotate': False,
        'specific_light': False,
        'output_suffix': 'R1',
    }
    ROUND2 = {
        'num_tables': 3,
        'rotate': True,
        'specific_light': True,
        'output_suffix': 'R2',
    }

# 通过命令行参数或配置文件切换
parser.add_argument('--round', type=int, choices=[1, 2], required=True)
```

### 5.6 模型加载时间优化（P1 - 2025新规）

2025新规："考察赛队模型大小，将模型加载时间计入总的识别分"

优化方案：
1. **模型量化**: INT8量化将~6MB模型压缩至~1.5MB
2. **预加载策略**: 系统启动时预加载模型到内存，start.sh仅触发推理
3. **模型格式优化**: 使用NPU原生格式(.om)替代PyTorch(.pt)，减少加载和反序列化时间
4. **TensorRT/ONNX Runtime**: 用ONNX Runtime替代PyTorch加载，减少Python overhead

---

## 六、优先级行动计划

### P0（致命缺陷，必须立即修复）

| 序号 | 任务 | 预计工作量 | 状态 |
|------|------|-----------|------|
| 0a | **NPU 实时画面检测框不渲染** | 0.5天 | 🔧 已知根因,待修复 |
| 0b | **NPU 深度对齐 pFrame nullptr 过滤帧** | 0.5天 | 🔧 已修,待验证 |
| 0c | **NPU letterbox 坐标还原精度** | 0.5天 | 🔧 已实现,待 GUI 验证 |
| 1 | 添加Table编号到输出格式 | — | ✅ |
| 2 | 集成OCR文字识别模块 | — | ✅ |
| 3 | **扩充训练数据集至18类** | 7天 | 🔜 最高优先 |
| 4 | 实现相机旋转控制协议 | 1天 | ⚠️ 框架完成 |

### P1（高优先级，显著提升得分）

| 序号 | 任务 | 预计工作量 | 状态 |
|------|------|-----------|------|
| 5 | **NPU推理加速** | — | ✅ 20.4ms/帧, 28× |
| 5a | NPU 实时画面标签渲染修复 | 0.5天 | 🔧 下个会话 |
| 5b | 替换队名 (光锥 LightCone) | 0.25天 | 🔜 全局搜索替换 |
| 6 | 裁判盒评分闭环测试 | 1天 | 🔜 需 Excel + 真值匹配 |
| 7 | 两轮比赛自适应配置 | — | ✅ |
| 8 | Round 1 端到端完整测试 | 1天 | 🔜 放入实际物品跑通 |
| 9 | 数据增强+多背景/光照训练 | 5天 | 依赖P0-3 |

### 已知 Bug 清单 (2026-07-14)

| # | Bug | 现象 | 优先级 |
|---|-----|------|:--:|
| B1 | **NPU 实时画面无检测框** | 右侧 list 有结果但画面上无标签/框 | P0 |
| B2 | **NPU 深度对齐丢帧** | 大量 pFrame nullptr 警告, 彩色帧被跳过 | P0 |
| B3 | **多帧投票结果为空** | NEEPU-VF-R1.txt 仅 10 字节, START/END 无内容 | P0 |
| B4 | **CB001 巧克力从未检出** | best4.pt 训练数据与实物不匹配 | P1 |
| B5 | **相机 USB 2.0 模式** | 未插蓝色 USB 3.0 口, 帧率受限 | P2 |
| B6 | **OCR 未安装** | Tesseract + pytesseract 缺失 | P2 |
| B7 | **NPU → CPU 回退后 close segfault** | `__del__` 清理顺序问题, 不影响推理 | P3 |
| 7 | 两轮比赛自适应配置 | 2天 | - |
| 8 | 模型加载时间优化 | 3天 | 依赖P1-5 |
| 9 | 数据增强+多背景/光照训练 | 5天 | 依赖P0-3 |

### P2（中优先级，锦上添花）

| 序号 | 任务 | 预计工作量 | 依赖 |
|------|------|-----------|------|
| 10 | RGBD双模态融合 | 7天 | - |
| 11 | 模型集成(3模型投票) | 3天 | 依赖P0-3 |
| 12 | 知识蒸馏(YOLO11s→n) | 3天 | 依赖P0-3 |
| 13 | 自动相机参数管理 | 1天 | - |

---

## 七、与GitHub开源项目的协作建议

建议获取以下资源辅助开发：

1. **裁判盒协议文档**: 通过QQ群(1027375571)获取最新通讯协议
2. **往年比赛录屏**: 搜索B站/YouTube上的"中国机器人大赛 先进视觉"视频
3. **GitHub相似项目**:
   - xensedyl/crc2025_3d_detect_dyl (已获取)
   - 搜索 "RoboCup China 3D recognition" / "中国机器人大赛 3D识别"
4. **数据集**: 可能需要自行采集，参考ImageNet/Open Images中的生活物品子集
5. **PaddleOCR**: https://github.com/PaddlePaddle/PaddleOCR (轻量OCR方案)
6. **Ascend工具链**: 华为CANN社区 https://www.hiascend.com/

---

## 八、总结

### 当前完成度估算（2026-07-14 更新）

| 维度 | 完成度 | 变化 | 说明 |
|------|--------|------|------|
| **硬件配置** | 95% | ↑5% | 相机+计算平台就位，**HDMI 桌面实测通过** |
| **基础检测** | **44%** | — | 模型仅8类，实测 `no detections` 待排查 |
| **规则合规** | **80%** | ↑5% | Table✅ OCR✅ 旋转框架✅ 两轮自适应✅ 录制✅ 裁判盒协议✅ **Round 1 全流程跑通** |
| **得分优化** | 25% | — | 模型压缩、NPU加速、时间分优化待做 |
| **环境部署** | **95%** | NEW | opencv/Qt/DISPLAY/路径 全部已解决 |
| **整体评估** | **~55%** | ↑5% | 环境已就绪，核心瓶颈仍在模型类别不足 |

### 2026-07-14 实测部署记录

**已解决的环境问题**：
1. `opencv-python-headless` → `opencv-python` (cv2.imshow 可用)
2. opencv Qt 插件与 PyQt5 冲突 → `rm -rf cv2/qt/` 解决
3. SSH→HDMI GUI 显示 → `DISPLAY=:0` 环境变量
4. 裁判盒 IP → 192.168.1.66 (Windows PC)
5. 部署路径 → `~/Desktop/Round/ultralytics-main/`
6. `识别.sh` 增强: ping 检查 + 模型检查 + DISPLAY=:0

**实测指标**：
- ARM CPU 单帧推理: ~568ms (no detections)
- 相机: USB 2.0 模式 (建议换 USB 3.0 蓝色口)
- 帧同步: 不支持 (Astra Pro Plus 硬件限制, 非致命)

### 2026-07-13 训练模型扫描结果

**模型权重扫描**:

| 权重文件 | 大小 | 类别数 | 实际类别 |
|----------|------|--------|----------|
| best4.pt | 6.0 MB | **8** | CA001,CA002,CB001,CB002,CC001,CC002,CD001,CD002 |
| best5.pt | 6.0 MB | **8** | 同上 |
| best6.pt | 6.0 MB | **8** | 同上 |
| bbest.pt | 6.0 MB | **8** | 同上 |
| bestt.pt | 6.0 MB | **8** | 同上 |

**训练配置**: `crc2026_18class.yaml` — 18类定义完备
**数据集**: `dataset/images/train/` 和 `val/` — **均为空目录(0张图片)**
**采集指南**: `dataset/README.md` — 详尽文档，目标 ~3500 张照片

**缺失类别**: CA003(水杯), CA004(衣架), CB003(火腿肠), CB004(薯片),
             CC003(盒装牛奶), CC004(瓶装水), CD003(香蕉), CD004(梨),
             W001, W002 (未知物品)

### 已完成 vs 待完成

**✅ 已完成**：
1. Table编号输出格式 → `Goal_ID=xxx;Num=n;Table=n`
2. OCR文字识别模块 → LightweightOCR(Tesseract)，零训练开箱即用
3. RoundConfig两轮自适应框架 → `--round 1|2`
4. 相机旋转协议框架 → `send_rotate_command()`（协议格式待确认）
5. 识别.sh传参 → `./识别.sh 1` / `./识别.sh 2`
6. 资格认证视频录制 → `record_video_opi.py`
7. **CANN/AscendCL 环境安装** → NPU 加速第①步完成

**🔜 下一步必做**：
1. **16类模型训练** — YOLO11s预训练 + 比赛物品微调
2. **QQ群确认** — 旋转指令格式 + MinTime/MaxTime/MinProp参数
3. **OCR实地测试** — 香橙派上跑Tesseract验证速度和准确率
4. **端到端集成测试** — Round 1 + Round 2 全流程模拟

**📋 远期优化**：
1. NPU推理加速（YOLO→ONNX→Ascend .om）
2. 模型加载时间优化（INT8量化）
3. OCR多尺度预处理 + 多帧投票
4. RGBD双模态融合
